# Zugangsdaten — NIEMALS ins Repository!
# Hier landet das Kennwort der App beim lokalen Betrieb.
.streamlit/secrets.toml
*.env
.env

# Zwischenspeicher des Scanners (Kursdaten, wird täglich neu geholt)
.cache/

# Zustand des Wächters und Anmeldedaten des Alarm-Bots
# (session.json enthält Sitzungs-Cookies — NIEMALS einchecken!
#  fortschritt.json dagegen ist seit 27.07.2026 BEWUSST im Repo:
#  nur Ticker|Nr|Preis-Schlüssel, keine Geheimnisse — das
#  Gesetzt-Gedächtnis muss Cache-Verluste überleben.)
session.json
watcher_state.json

# Diagnosematerial des Alarm-Bots (Screenshots, HTML-Auszüge)
debug/

# Ergebnisse
kaufpunkte.xlsx

# Python-Krempel
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
# PowerShell legt bei "> `$null" versehentlich eine Datei dieses Namens an
$null
